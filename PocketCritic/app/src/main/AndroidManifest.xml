<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.tufts.edu.pocketcritic">

    <!-- To auto-complete the email text field in the login form with the user's emails -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArtisitsActivity" />
        <activity android:name=".ActivitybioActivity" />
        <activity android:name=".AlbumlistActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".NavigateActivity" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="caf810954f358a2c70a15aae13fd2d49a326d47d" />
    </application>

</manifest>